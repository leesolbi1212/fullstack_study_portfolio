// const age = 20;
// const message = age >= 18 ? "성인" : "미성년자";

// console.log(message);

const score = 85;
const grade =
  score >= 90
    ? "A"
    : score >= 80
    ? "B"
    : score >= 70
    ? "C"
    : score >= 60
    ? "D"
    : "F";
console.log(`당신의 등급은 ${grade}입니다!`);
